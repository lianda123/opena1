name: Build WoodCheck for Rhino 7 and 8

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
    paths:
      - "WoodCheck/**"
      - ".github/workflows/build-wood-check.yml"
  push:
    branches:
      - main
    paths:
      - "WoodCheck/**"
      - ".github/workflows/build-wood-check.yml"

permissions:
  contents: read

jobs:
  build:
    runs-on: windows-latest
    timeout-minutes: 20

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Verify WoodCheck rules
        shell: pwsh
        working-directory: WoodCheck
        run: python tests/verify_woodcheck.py

      - name: Build WoodCheck
        shell: pwsh
        working-directory: WoodCheck
        run: ./build.ps1 -Configuration Release

      - name: Upload Rhino 7 plug-in
        uses: actions/upload-artifact@v4
        with:
          name: WoodCheck-1.0.0-rhino7
          path: WoodCheck/dist/WoodCheck-1.0.0-rhino7.zip
          if-no-files-found: error

      - name: Upload Rhino 8 plug-in
        uses: actions/upload-artifact@v4
        with:
          name: WoodCheck-1.0.0-rhino8
          path: WoodCheck/dist/WoodCheck-1.0.0-rhino8.zip
          if-no-files-found: error

      - name: Upload combined package
        uses: actions/upload-artifact@v4
        with:
          name: WoodCheck-1.0.0-rhino7-rhino8
          path: WoodCheck/dist/WoodCheck-1.0.0-rhino7-rhino8.zip
          if-no-files-found: error
